import { FiltroAlumnosPipe } from './filtro-alumnos.pipe';

describe('FiltroAlumnosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAlumnosPipe();
    expect(pipe).toBeTruthy();
  });
});
